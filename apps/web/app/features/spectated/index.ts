/**
 * The battles this account watched rather than played: the home page's
 * spectated section.
 *
 * The public API of `features/spectated`. See `features/stats/index.ts` for
 * what that means.
 */
export { useSpectatedBattles } from './composables/useSpectatedBattles'
