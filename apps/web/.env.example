# Copy to apps/web/.env. Both values are printed by `pnpm db:start`; the anon
# key is meant to be public — RLS is what guards the data, not this key.
NUXT_PUBLIC_SUPABASE_URL="http://127.0.0.1:54321"
NUXT_PUBLIC_SUPABASE_ANON_KEY=""
