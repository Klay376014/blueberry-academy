// The parser is plain TypeScript with no DOM and no framework: it must stay a
// pure function so it can be tested from fixtures alone.
{
  "extends": "@tsconfig/node24/tsconfig.json",
  "include": ["src/**/*.ts", "test/**/*.ts"],
  "compilerOptions": {
    "module": "preserve",
    "moduleResolution": "bundler",
    // Explicit .ts extensions so bare `node` can load the parser: scripts/reparse.ts
    // runs under Node's own type stripping, which does no extension guessing.
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "types": [],
    "noEmit": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.tsbuildinfo"
  }
}
